<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <appender name="Log Back Log" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- //... -->
    </appender>

    <root level="info">
        <appender-ref ref="Log Back Log"/>
    </root>

</configuration>